#include <time.h>
#include "settings.h"

void setup() {
  Serial.begin(SERIAL_BAUD);
  // Send empty boardmessage
}

void loop() {

  // get message from Comms
  // if message there
  //    send to executor
  // else
  //    delay
  // fi

}
